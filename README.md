# 👟 Sneaker Shop

Полнофункциональный интернет-магазин кроссовок с каталогом товаров, корзиной, оплатой через Stripe, авторизацией пользователей и отзывами. Построен на современном стеке Next.js 16 с серверными компонентами и App Router.

![Next.js](https://img.shields.io/badge/Next.js-16-black?logo=next.js)
![React](https://img.shields.io/badge/React-19-61DAFB?logo=react)
![TypeScript](https://img.shields.io/badge/TypeScript-5-3178C6?logo=typescript)
![Prisma](https://img.shields.io/badge/Prisma-7-2D3748?logo=prisma)
![Stripe](https://img.shields.io/badge/Stripe-Payments-635BFF?logo=stripe)

---

## 📋 Содержание

- [Возможности](#-возможности)
- [Технологический стек](#-технологический-стек)
- [Архитектура проекта](#-архитектура-проекта)
- [Быстрый старт](#-быстрый-старт)
- [Переменные окружения](#-переменные-окружения)
- [Скрипты](#-скрипты)
- [Тестирование](#-тестирование)
- [Для разработчиков](#-для-разработчиков)
- [Лицензия](#-лицензия)

---

## ✨ Возможности

- **Каталог товаров** — просмотр кроссовок с фильтрацией по брендам и категориям
- **Страница товара** — детальная информация, выбор размера, галерея изображений
- **Корзина** — добавление / удаление товаров, управление количеством (Zustand)
- **Оплата** — безопасная оплата через Stripe Checkout
- **Авторизация** — регистрация и вход через NextAuth v5 (email + пароль, bcrypt-хеширование)
- **Отзывы** — авторизованные пользователи могут оставлять оценки и комментарии
- **Заказы** — история заказов со статусами (PENDING → PAID → SHIPPED → DELIVERED)
- **Анимации** — плавные переходы и анимации через Framer Motion
- **Адаптивный дизайн** — корректное отображение на мобильных устройствах

---

## 🛠 Технологический стек

### Фронтенд

| Технология | Версия | Назначение |
|---|---|---|
| **Next.js** | 16.1 | Фреймворк с App Router и серверными компонентами |
| **React** | 19.2 | UI-библиотека |
| **TypeScript** | 5.x | Статическая типизация |
| **Zustand** | 5.x | Управление состоянием (корзина) |
| **Framer Motion** | 12.x | Анимации и переходы |
| **CSS Modules** | — | Стилизация компонентов |

### Бекенд

| Технология | Версия | Назначение |
|---|---|---|
| **Next.js API Routes** | 16.1 | REST API (App Router route handlers) |
| **Prisma ORM** | 7.x | Работа с базой данных |
| **PostgreSQL** | — | Реляционная база данных |
| **NextAuth.js** | 5 beta | Аутентификация (Credentials provider) |
| **bcryptjs** | 3.x | Хеширование паролей |
| **Stripe** | 20.x | Приём платежей |

### Тестирование

| Технология | Назначение |
|---|---|
| **Jest** | Тестовый фреймворк |
| **React Testing Library** | Тестирование компонентов |
| **ts-jest** | Поддержка TypeScript в Jest |

---

## 🏗 Архитектура проекта

```
sneaker-shop/
├── prisma/
│   └── schema.prisma          # Схема БД (User, Product, Order, Review)
├── public/
│   └── products/              # Изображения товаров
├── src/
│   ├── app/                   # Next.js App Router
│   │   ├── api/               # API-роуты (checkout, webhook и др.)
│   │   ├── about/             # Страница «О нас»
│   │   ├── brands/            # Каталог брендов
│   │   ├── cart/              # Корзина
│   │   ├── checkout/          # Оформление заказа и подтверждение
│   │   ├── login/             # Авторизация
│   │   ├── product/[slug]/    # Детальная страница товара
│   │   ├── shop/              # Каталог товаров
│   │   ├── layout.tsx         # Корневой layout
│   │   ├── page.tsx           # Главная страница
│   │   └── globals.css        # Глобальные стили
│   ├── components/            # Переиспользуемые компоненты
│   │   ├── Footer/            # Подвал сайта
│   │   ├── Navbar/            # Навигация
│   │   ├── ProductCard/       # Карточка товара
│   │   ├── Motion/            # Обёртки для анимаций
│   │   └── Providers/         # React-провайдеры (Session, etc.)
│   ├── data/                  # Статические данные (seed-данные товаров)
│   ├── lib/                   # Утилиты и конфигурации
│   │   ├── auth.ts            # Конфигурация NextAuth
│   │   ├── prisma.ts          # Singleton Prisma Client
│   │   └── stripe.ts          # Инициализация Stripe SDK
│   └── store/
│       └── cart.ts            # Zustand-стор корзины
├── __tests__/                 # Тесты
│   ├── api/                   # Тесты API-эндпоинтов
│   ├── data/                  # Тесты данных о продуктах
│   ├── security/              # Тесты безопасности (auth)
│   └── store/                 # Тесты Zustand-стора
├── jest.config.ts             # Конфигурация Jest
├── jest.setup.ts              # Настройка тестового окружения
├── next.config.ts             # Конфигурация Next.js
├── tsconfig.json              # Конфигурация TypeScript
└── .env.example               # Пример переменных окружения
```

### Модели данных (Prisma)

```
User ──< Order ──< OrderItem >── Product ──< ProductSize
  │                                  │
  └──< Review >─────────────────────┘
```

- **User** — пользователь (роли: `USER`, `ADMIN`)
- **Product** — товар (название, цена, бренд, цвета, изображения)
- **ProductSize** — размер + остаток на складе
- **Order** — заказ (статусы: `PENDING` → `PAID` → `SHIPPED` → `DELIVERED` → `CANCELLED`)
- **OrderItem** — позиция заказа (товар, размер, количество, цена)
- **Review** — отзыв (рейтинг, комментарий; один отзыв на пользователя на товар)

---

## 🚀 Быстрый старт

### Требования

- **Node.js** ≥ 18
- **npm** ≥ 9 (или yarn / pnpm)
- **PostgreSQL** — запущенный экземпляр
- **Stripe-аккаунт** — для приёма платежей (тестовый режим)

### Установка

```bash
# 1. Клонировать репозиторий
git clone https://github.com/<username>/sneaker-shop.git
cd sneaker-shop

# 2. Установить зависимости
npm install

# 3. Настроить переменные окружения
cp .env.example .env.local
# Заполните .env.local вашими значениями (см. раздел ниже)

# 4. Сгенерировать Prisma Client
npx prisma generate

# 5. Применить миграции к базе данных
npx prisma db push

# 6. (Опционально) Заполнить базу тестовыми данными
npx prisma db seed

# 7. Запустить сервер разработки
npm run dev
```

Приложение будет доступно по адресу: **http://localhost:3000**

---

## 🔐 Переменные окружения

Создайте файл `.env.local` на основе `.env.example`:

| Переменная | Описание | Пример |
|---|---|---|
| `DATABASE_URL` | Строка подключения к PostgreSQL | `postgresql://user:password@localhost:5432/sneaker_shop` |
| `NEXTAUTH_URL` | URL приложения | `http://localhost:3000` |
| `NEXTAUTH_SECRET` | Секретный ключ для подписи JWT | Сгенерируйте: `openssl rand -base64 32` |
| `NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY` | Публичный ключ Stripe | `pk_test_...` |
| `STRIPE_SECRET_KEY` | Секретный ключ Stripe | `sk_test_...` |
| `STRIPE_WEBHOOK_SECRET` | Секрет вебхука Stripe | `whsec_...` |

> ⚠️ **Важно:** Никогда не коммитьте файл `.env.local` в репозиторий. Он уже добавлен в `.gitignore`.

---

## 📜 Скрипты

| Команда | Описание |
|---|---|
| `npm run dev` | Запуск сервера разработки с hot-reload |
| `npm run build` | Сборка продакшен-версии |
| `npm start` | Запуск собранной продакшен-версии |
| `npm run lint` | Линтинг кода (ESLint) |
| `npm test` | Запуск тестов (Jest) |
| `npm run test:watch` | Запуск тестов в watch-режиме |
| `npm run test:coverage` | Запуск тестов с отчётом покрытия |

---

## 🧪 Тестирование

Тесты располагаются в директории `__tests__/` и организованы по категориям:

```
__tests__/
├── api/          # Тесты API-эндпоинтов (checkout и др.)
├── data/         # Валидация данных о продуктах
├── security/     # Тесты безопасности (аутентификация, XSS, инъекции)
└── store/        # Тесты Zustand-стора корзины
```

```bash
# Запуск всех тестов
npm test

# Запуск конкретной категории
npx jest __tests__/security

# Запуск с покрытием
npm run test:coverage
```

---

## 👩‍💻 Для разработчиков

### Как начать работу с проектом

1. Сделайте **fork** репозитория
2. Создайте ветку для вашей фичи: `git checkout -b feature/my-feature`
3. Установите зависимости и настройте окружение (см. [Быстрый старт](#-быстрый-старт))
4. Внесите изменения и убедитесь, что тесты проходят: `npm test`
5. Создайте Pull Request с описанием изменений

### Работа с базой данных

```bash
# Открыть Prisma Studio (визуальный редактор БД)
npx prisma studio

# Создать новую миграцию после изменения schema.prisma
npx prisma migrate dev --name описание_изменений

# Сбросить БД и применить миграции заново
npx prisma migrate reset
```

### Работа со Stripe (тестовый режим)

Для локальной разработки с вебхуками Stripe:

```bash
# Установите Stripe CLI: https://stripe.com/docs/stripe-cli
stripe listen --forward-to localhost:3000/api/webhook
```

Stripe CLI выведет `whsec_...` — используйте его в `STRIPE_WEBHOOK_SECRET`.

### Структура и соглашения

- **App Router** — все страницы в `src/app/`, каждая папка = роут
- **CSS Modules** — стили рядом с компонентом: `page.module.css`
- **Server Components** — по умолчанию все компоненты серверные; `"use client"` только где необходимо
- **Prisma Singleton** — используйте `import { prisma } from '@/lib/prisma'` для доступа к БД
- **Zustand** — клиентское состояние (корзина) в `src/store/cart.ts`

### Полезные ресурсы

- [Next.js Documentation](https://nextjs.org/docs)
- [Prisma Documentation](https://www.prisma.io/docs)
- [NextAuth.js Documentation](https://authjs.dev)
- [Stripe Documentation](https://stripe.com/docs)
- [Zustand Documentation](https://docs.pmnd.rs/zustand)
- [Framer Motion Documentation](https://motion.dev)

---

## 📄 Лицензия

Этот проект распространяется под лицензией MIT. Подробности см. в файле [LICENSE](LICENSE).
